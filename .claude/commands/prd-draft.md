---
name: prd-draft
description: Generate a complete first-draft PRD from a context file or inline brief
argument-hint: "<path-to-context.md or inline brief> [--format 1pager|pitch|ai]"
allowed-tools: Read, Write
---

Read `.claude/prd/prd-draft/SKILL.md` and execute its full instructions with `$ARGUMENTS` as the input (context file path or inline brief, with optional --format flag).
