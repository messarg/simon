---
name: prd-discover
description: Run a structured discovery session to gather context before writing a PRD
argument-hint: <feature-name>
allowed-tools: Read, AskUserQuestion, Write
---

Read `.claude/prd/prd-discover/SKILL.md` and execute its full instructions with `$ARGUMENTS` as the feature name input.
