/** Sale-return arithmetic lives in @simon/shared so the till previews exactly what the server posts (§12.4). */
export { computeReturn, type OriginalLine, type OriginalSale, type ReturnLineResult, type ReturnRequestLine, type ReturnResult } from "@simon/shared";
